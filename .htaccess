Options -MultiViews
RewriteEngine On

# Keep built assets and common files addressable.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Let TanStack Router handle client-side routes on shared hosting.
RewriteRule ^ index.html [L]
